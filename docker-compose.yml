services:
  keyhunter:
    build: .
    container_name: keyhunter-daemon
    volumes:
      - ./config.toml:/app/config.toml:ro
      - keyhunter-data:/app/results
    working_dir: /app
    command: ["daemon", "--config", "/app/config.toml"]
    restart: unless-stopped

volumes:
  keyhunter-data:
